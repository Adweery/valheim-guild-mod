name: Installer tests
on: [push, workflow_dispatch]
permissions:
  contents: read
jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test Windows PowerShell installer in temporary game folders
        run: python -m unittest discover -s tests -v
